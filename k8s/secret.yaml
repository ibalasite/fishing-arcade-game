# IMPORTANT: Replace all values with real secrets before deploying.
# These placeholder values are base64("REPLACE_ME") and must never reach production.
# Use a secrets manager (Vault, AWS Secrets Manager, GCP Secret Manager) or
# an External Secrets Operator to inject real values at deploy time.
apiVersion: v1
kind: Secret
metadata:
  name: fishing-game-secrets
  namespace: fishing-game
  labels:
    app: fishing-game-server
  annotations:
    # Reminder: rotate secrets via the procedure documented in k8s/README.md
    secret-rotation-procedure: "see k8s/README.md"
type: Opaque
data:
  JWT_SECRET: UkVQTEFDRV9NRQ==          # base64("REPLACE_ME")
  DATABASE_URL: UkVQTEFDRV9NRQ==         # base64("REPLACE_ME")
  REDIS_URL: UkVQTEFDRV9NRQ==            # base64("REPLACE_ME")
  ENCRYPTION_KEY: UkVQTEFDRV9NRQ==       # base64("REPLACE_ME")
  HMAC_SECRET_KEY: UkVQTEFDRV9NRQ==      # base64("REPLACE_ME")
